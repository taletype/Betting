[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && pnpm install && cd services/api && pnpm build"

[deploy]
startCommand = "pnpm --filter @bet/service-api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
