[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && pnpm install && cd services/api && pnpm build"

[deploy]
startCommand = "node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
