{
  "name": "@bet/ledger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json",
    "test": "pnpm exec node --import tsx --test src/journals/helpers.test.ts"
  },
  "dependencies": {
    "@bet/domain": "workspace:*"
  }
}
